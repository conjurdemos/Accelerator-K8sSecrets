# Applied at: /data
# Grants the workload group access to secrets synced from safe {{SAFE_NAME}}
- !grant
  role: !group vault/{{SAFE_NAME}}/delegation/consumers
  member: !group {{JWT_SERVICE_ID}}/workloads
